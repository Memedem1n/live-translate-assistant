# Gun Sonu Notu - 4 Mart 2026

## Bugun yapilanlar

1. STT runtime tarafi daha dayanikli hale getirildi:
   - `auto/cuda/cpu` runtime mode
   - worker warmup + ready akisinin netlestirilmesi
   - cuda retry ve otomatik cpu fallback
   - runtime status event akisinin UI'ye tasinmasi
2. Interview asistan akisinda guvenilirlik ve kullanilabilirlik iyilestirmeleri yapildi.
3. Profil/personalization temel yapisi genisletildi:
   - kaynak tipleri, import/sync akislari, context preview ve reindex
4. Finetune hazirlik pipeline'i eklendi ve script seti tamamlandi:
   - web corpus sync
   - glossary build
   - dataset build
   - LoRA train config/attempt orchestration
   - env check + ollama package hazirlik
5. Runtime ve train ortamlarini ayri venv ile yonetmek icin:
   - `setup_train_env.ps1`
   - `run_train_python.ps1`
   - `package.json` script guncellemeleri
6. Dataset source-mix mantigi guclendirildi:
   - enforcement toggle
   - glossary oran dengelemesi
7. Disk temizligi yapildi:
   - gereksiz build/artifact klasorleri silindi
   - kullanilmayan modeller silindi (`qwen2.5:3b`, `qwen2.5:7b`)

## Dogrulama ozeti

- `npm run typecheck`: gecti
- `npm run test`: gecti
- corpus/glossary/dataset smoke: gecti
- LoRA prepare smoke: config/attempt raporu olusumu dogrulandi
- `finetune:check-env`: su an fail (train venv paketleri tam yuklu degil)

## Bugun yapilmayanlar

1. `finetune:setup-env` tam kurulumunun bitirilmesi
2. Gercek LoRA full training kosusu
3. D diske kalici migration adimi

## Mevcut durum (gun sonu)

- Branch: `eod/2026-03-04`
- Hedef push: 2 commit
  - Commit 1: runtime/ui/diagnostics
  - Commit 2: personalization/finetune/scripts/docs
- Aktif model seti:
  - Ollama: `qwen2.5:14b-instruct-q4_K_M`
  - STT cache: `faster-whisper-large-v3`

## Yarin ilk adimlar

1. `npm run finetune:setup-env`
2. `npm run finetune:check-env` (`ok=true` hedefi)
3. `npm run finetune:prepare-lora:smoke`
4. Sonuca gore full train ve profiling
5. Ardindan D disk migration fazi
