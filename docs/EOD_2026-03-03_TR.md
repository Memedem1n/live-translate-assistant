# Gun Sonu Notu - 3 Mart 2026

## Bugun tamamlananlar

1. Uygulama arayuzu sadeleştirildi (minimal kontrol panel, advanced altinda opsiyonel ayarlar).
2. Overlay dynamic-island benzeri ust-orta akisa cevrildi.
3. Kontrol paneli gizleme + tray geri acma akisi eklendi.
4. System audio varsayilan olarak otomatik secime alindi, manual override advanced altina tasindi.
5. Runtime otomasyon scriptleri eklendi:
   - `npm run runtime:setup`
   - `npm run models:pull`
   - `npm run clips:prepare`
   - `npm run stt:prewarm`
   - `npm run benchmark:sweep`
   - `npm run demo:pipeline`
6. LLM modeller indirildi:
   - `qwen2.5:3b-instruct-q4_K_M`
   - `qwen2.5:7b-instruct-q4_K_M`
   - `qwen2.5:14b-instruct-q4_K_M`
7. STT benchmark/sweep otomasyonu calistirildi ve raporlar uretildi.

## Son metrik ozeti

- Referans rapor: `benchmark/reports/sweep_20260303_003611.json`
- `small.en + qwen2.5:3b-instruct-q4_K_M`:
  - STT p50: `2170.57 ms`
  - Assist first token p50: `146.86 ms`
  - Assist final p50: `823.36 ms`
- Latency gate sonucu: **gecemedi** (STT p50 hedefi `<= 800 ms` saglanmadi).

## Acik blokaj

- STT tarafi CUDA secse bile transkripsiyon adiminda CPU fallback yapiyor.
- Log:
  - `Library cublas64_12.dll is not found or cannot be loaded`
- CUDA 12.6 kuruldu, `cublas64_12.dll` dosyasi mevcut; buna ragmen runtime zinciri eksik/uyumsuz gorunuyor.

## Yarin ilk isler

1. STT CUDA zincirini kesin coz:
   - `dumpbin /dependents` veya benzeri ile eksik bagimli DLL’i tespit et.
   - Gerekli cuDNN/cuBLAS runtime paketlerini tamamla.
   - `small.en` icin zorunlu CUDA testini izole script ile dogrula.
2. CUDA aktif oldugunda 1-konfig sweep’i tekrar al:
   - Hedef: STT p50 degerini ciddi dusurmek.
3. Hedefler yine gecmezse:
   - Canli akis odakli STT stratejisi (chunking/VAD) ile model-kalibrasyon turu yap.
4. Disk tasima:
   - Proje + model/cache yollarini D diskine tasima planini uygula.
